#import googlemaps

class Maps:

    def __init__(self):
       # self.gmaps = googlemaps.Client(key='AIzaSyBqtbWLMUfuX0e9DcirWwY1fpSjiwS8BeU')
       x =1

    def draw_route(self, routePoints):
        print(routePoints)
        # points = []
        # bounds = self.gmaps.LatLngBounds()
        
        # for item in routePoints:
        #     latlon = self.gmaps.LatLng(item[3], item[4])
        #     points.append(latlon)
        #     bounds.extend(latlon)

        # self.gmaps.fitBounds(bounds)

        # mappedTrail = self.gmaps.maps.Polilyne(
        #     path= points,
        #     geodesic= true,
        #     strokeColor= routeColor,
        #     strokeOpacity= 1.0,
        #     strokeWeight= 2
        # )

        # mappedTrail.setMap(self.gmaps)

        # self.gmaps.maps.setCenter(bounds.getCenter())
